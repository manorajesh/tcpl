#include <stdio.h>

int main(void) {
  
  /*double c;
  int i;

  while ((c = getchar()) != EOF)
  {
    if (c == i)
      printf("%f is a perfect square\n", c);
    else
      printf("No, %f is not a perfect square", c);
  }
  */

  int c, i;
  int perSqrt[1000];
  for

}